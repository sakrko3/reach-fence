type Props = {};

const ZeroContact = (props: Props) => {
  return (
    <section className="min-h-screen flex justify-center items-center">
      <p>No contacts saved.</p>
    </section>
  );
};

export default ZeroContact;
